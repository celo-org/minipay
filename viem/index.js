const { config } = require("dotenv");
config();

async function main() {
    // Use Code Snippets here
}

main()
    .then((result) => console.log(result))
    .catch((err) => {
        console.error(err);
        process.exit(1);
    });
